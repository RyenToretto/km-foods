---
description: 新增、修改或删除菜谱后同步更新 prepare.md
globs: recipes/**/README.md
alwaysApply: false
---

# prepare.md 同步规则

当新增、修改或删除 `recipes/` 下的菜谱后，必须同步更新根目录的 `prepare.md`：

1. 扫描所有 `recipes/**/README.md` 中的用具、调料、食材
2. 合并同义项（如 姜/生姜 → 姜，大葱/小葱 分开保留）
3. 按使用次数降序排列，常用在前
4. 用具、调料各一张表；食材按类别（蛋奶/肉禽/海鲜/蔬菜/主食干货/水果/豆制品坚果/饮品原料）分表
5. 每项标注使用次数和常见用途
